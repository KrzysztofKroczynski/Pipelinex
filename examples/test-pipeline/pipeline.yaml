name: test-pipeline
version: 1.0.0

model:
  provider: anthropic
  name: claude-sonnet-4-6
  api_key: ${ANTHROPIC_API_KEY}

steps:
  - id: step-01-ingest

  - id: step-02-process
    max_iterations: 100
    can_goto:
      - step-02-process
      - step-03-output

  - id: step-03-output
    terminal: true
